@model IEnumerable<Joyeria.Models.GenderType>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center heading-font">View All Gender Types</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Add New Gender Type</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenderTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenderTypeName)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-secondary" asp-route-id="@item.GenderTypeID">Edit</a> |
                <a asp-action="Details" class="btn btn-success" asp-route-id="@item.GenderTypeID">Details</a> |
                <a asp-action="Delete"  class="btn btn-danger" asp-route-id="@item.GenderTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
